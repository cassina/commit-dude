# Commit Dude 🧠🤖

Commit Dude is a friendly command line helper that turns your staged or working tree changes into polished
[Conventional Commit](https://www.conventionalcommits.org/) messages.

## ✨ Features
- Generates Conventional Commit compliant messages from any git diff.
- Automatically includes `git status --porcelain` output for additional context.
- Copies the suggested message to your clipboard using `pyperclip`.
- Works as a standalone CLI (`commit-dude`) or through `python -m commit_dude`.
- Wraps commit subjects and bodies to a 100-character width so long lines are automatically reflowed.

## 📦 Requirements
- Python 3.10 or newer.
- [uv](https://github.com/astral-sh/uv) for dependency management and packaging.
- An OpenAI API key with access to the `gpt-4o-mini` model (set as `OPENAI_API_KEY`).

## ⚙️ Installation
```bash
uv add commit-dude
```

## 🔑 Configuration
You must have an OpenAI API key in your environment.

```bash
OPENAI_API_KEY="sk-..."
```

## 🚀 Usage

```bash
uv run commit-dude
```

```bash
git diff | uv run commit-dude
```

By default, the CLI runs in **strict** mode and blocks generation if secret patterns are detected in the
diff. Use the `--no-strict` flag to switch to redaction mode, which redacts detected secrets instead
of aborting the run:

```bash
uv run commit-dude --no-strict
```

If you need additional diagnostics (including middleware logs and detection decisions), enable debug
logging:

```bash
uv run commit-dude --debug
```

The output will be similar to:
```bash
🤖 Generating commit message...
Hey there! Here’s a chill commit message for you: 

docs: add AGENTS.md and update CONTRIBUTING.md

- Added AGENTS.md to remind contributors to read the CONTRIBUTING.md file.
- Updated various sections in CONTRIBUTING.md for clarity and accuracy.
- Modified Makefile to sync dependencies with locked versions.
- Cleaned up README.md for better readability and updated usage instructions.

✅ Suggested commit message copied to clipboard. 
```

The CLI will:
1. Capture the diff of your working tree against `HEAD`.
2. Send the diff (plus `git status --porcelain` output) to the LLM.
3. Print the conversational response and commit message.
4. Copy the final commit message to your clipboard.
5. If there are no changes, Commit Dude exits with a helpful error message.


## 🧑‍💻 Contributing
See [CONTRIBUTING.md](CONTRIBUTING.md) for development guidelines, commit conventions, and release steps.

## ⚠️ Disclaimer of Liability and Usage Risks

### **Use at Your Own Risk**
Commit Dude is provided **“as is”** without any warranties or guarantees. By using this tool, you acknowledge that you are transmitting your code **diff** to an external service (OpenAI’s API) for processing. The authors and contributors of Commit Dude assume **no responsibility or liability** for any outcomes, including potential data exposure, misuse, or damage resulting from the use of this software.

You use Commit Dude **entirely at your own risk**, and you are solely responsible for how you utilize the generated commit messages.

### **Third-Party Service and Data Exposure**
All diff processing — including secret detection and redaction — occurs **client-side** within your environment. Only once the diff is vetted locally will it be sent to the third-party LLM service (OpenAI) to generate a commit message.

We do **not** collect or transmit your code to any server under our control — it goes directly from your machine to OpenAI’s API (using your API key).

Please note:

- Sending code to OpenAI means it may be stored or processed under their **Terms of Service**.
- We cannot control or secure data once it leaves your machine.
- We disclaim liability for any unauthorized access, misuse, or retention of your code by the third-party provider.

👉 **Do not use this tool on confidential or proprietary code unless you have permission and are comfortable with OpenAI’s terms.**

### **No Warranty on Outputs**
Commit messages and responses are generated by an AI model and may not always be accurate, appropriate, or complete. The authors make **no warranty** that the suggested commit message will correctly describe your changes or adhere to any convention.

You must **review all output carefully** before using it.

We are not liable for:

- incorrect commit messages  
- missing essential details  
- unintended or inappropriate content  

Always ensure the generated commit message is correct for your change.

### **Secret Detection Limitations**
Commit Dude includes a secret-pattern detector that attempts to identify sensitive credentials in your diff before it is sent.

- In **strict mode** (default), the tool **blocks** the request if a secret pattern is detected.  
- In **non-strict mode** (not recommended), the detected secrets are **redacted** (replaced with placeholders), and the request continues.

However:

- Redaction is **not foolproof**  
- Only **known patterns** are detected  
- Not all sensitive values can be identified  

The authors are not responsible if any sensitive information not recognized by the detector is sent to the LLM.

👉 **You must audit your diff for sensitive data.**  
When in doubt, remove or mask content manually.

### **Compliance and Policies**
By using Commit Dude, you agree to follow:

- OpenAI’s **Terms of Service**  
- Your organization’s **security and data policies**  

We are not responsible for any violations of OpenAI’s terms or company security rules that arise from your use of this tool.

If you are working in a corporate environment, ensure that sending code to an external service does **not** violate internal security requirements or agreements.

### **No Affiliation with OpenAI**
Commit Dude is an independent open-source project and is **not affiliated with, endorsed by, or sponsored by OpenAI**.

All trademarks (e.g., “OpenAI”, “GPT-4”, etc.) belong to their respective owners.

We cannot guarantee the:

- availability  
- security  
- performance  

of OpenAI’s services.

### **Future Local LLM Support**
We are actively exploring the addition of a **local LLM mode** in a future release, which would allow Commit Dude to run entirely on your machine without transmitting diffs to any external service. This feature is not yet available and no timeline is guaranteed, but it reflects our long-term commitment to improved privacy and offline-first workflows.

### **Summary**
Use Commit Dude responsibly:

- Review all outputs  
- Keep your API key secure  
- Avoid sharing sensitive code or PII
- Understand that secret detection is a safety aid, **not** a guarantee  

The authors provide this tool in good faith, but all responsibility for its use lies with you, the end user.

There is no warranty, and we exclude ourselves from all liability to the fullest extent permitted by law.
